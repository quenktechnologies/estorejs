/** 
 *
 * Installer is the interface for the objects that install the extension components
 * of the system.
 * @interface
 * @alias Installer
 */

/**
 * install an extension.
 * @param {Extension} ext
 */
Installer.prototype.install = function() {};
