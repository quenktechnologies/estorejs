/**
 * Daemons are functions that are run on setInterval.
 * Use them to do periodic tasks that can afford to be delayed
 * when the application is under heavy load.
 *
 * @interface extension.daemon
 * @memberof extension
 */

/**
 * A function in MyNamespace (MyNamespace.myFunction).
 * @function myFunction
 * @memberof extension.daemon
 */
